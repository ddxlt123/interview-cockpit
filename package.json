{
  "name": "interview-cockpit",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "VITE_BASE_PATH=/interview-cockpit/ vite build",
    "check:cloud": "node scripts/check-cloud-config.mjs",
    "deploy": "npm run check:cloud && npm run build:pages && gh-pages -d dist",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "mammoth": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite-plugin-pwa": "latest",
    "vitest": "latest"
  }
}
